services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      CORS_ORIGINS: http://localhost:5500,http://127.0.0.1:5500
    volumes:
      - health-risk-data:/app/backend

volumes:
  health-risk-data:
